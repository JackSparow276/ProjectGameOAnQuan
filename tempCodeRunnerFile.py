if i == 2:
        #     hl_rect = pygame.Rect(x, start_y + 100, cell_size, cell_size)
        #     # Dùng inflate để tạo lề nhỏ bên trong ô
        #     pygame.draw.rect(screen, HIGHLIGHT, hl_rect.inflate(-4, -4))
